
const config = {

    //the api endpoints
    ENDPOINTS: {
        TASKS_LIST: '/tasks',
        TASKS_ADD: '/tasks',
        TASKS_DETAIL: '/tasks',    // + '/:id'
        TASKS_DELETE: '/tasks',    // + '/:id'
        TASKS_UPDATE: '/tasks',    // + '/:id'
    },

}
export default config;